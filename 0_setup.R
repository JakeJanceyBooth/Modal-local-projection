#packages, global settings, seeds, horizons (?), default bandwidth
#constants (?), simulation sizes (?), plotting options (?)

library(ggplot2)
library(vars)

SEED <- 12345
set.seed(SEED)

options(scipen = 999)
